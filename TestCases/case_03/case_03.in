case03=function() {
    char str1[] = "Hello";
    char str2[] = "Wor
ld";

    // 字符串连接
    char combined[20];
    strcpy(combined, str1);
    strcat(combined, " ");
    strcat(combined, str2);
    printf("Combined string: %s\n", combined);

    return 0;
}
