case04=function(integer N, integer K, double rc){
     if(N==1) return(0);
     integer 1KL=floor(K * rc);         #split N 
     if(KL < 1 || _KL > (K-1))
            KL %= 1; ///This is/ a comment
     else if (KL > .5*K)
            KL = ceiling(KL // 2.0)
		/*?*//*
     KR = K - KL

     integer N_L = ceiling(N * KL / K)      #split N
     integer _NR*/= N - NL
	/*
}
